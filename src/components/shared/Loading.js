import React from "react";
import { Text, View } from "react-native";

const Loading = () => {
  return (
    <View className="items-center justify-center h-full w-full">
      <Text className="text-leaf-300 font-bold">Loading...</Text>
    </View>
  );
};

export default Loading;
